<div class="container my-4">
    <div class="row d-flex justify-content-center">
      <div class="col my-1">
      	<div class="col text-center d-flex justify-content-center" >

         <%= bootstrap_form_tag url: "/jobs/search_with_category", method: :get do |form| %>

            <%= form.collection_select :category_id, Category.order(:name), :id, :name, {label: "Seleciona uma categoria"} %>
            <%= form.submit "Pesquisar pela categória selecionada", class:"btn btn-success" %>
         <% end %>
       </div>
      </div>
    </div>


      
</div>