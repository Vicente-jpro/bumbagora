<!DOCTYPE html>
<html lang="pt">
  <head>
    <title>Bumbagora - Emprego em Angola | Desenvolviemto de Sistemas</title>
    <meta name="description" content="Bumbagora - Emprego em Angola | Desenvolviemto de Sistemas">
    <meta name="keywords" content="vagas de emprego em angola, criação de web site, criação de aplicatios, business intelligence, desenvolvimento de sistemas, soluções em ti, luanda angola, criação de sites, tecnologia da informação, vagas de emprego, bumbagora, ruby, ruby on rails, ruby angola, ruby on rails angola, vicente, vicente simao, vicente on rails, senhor ruby angola">
    <meta name="author" content="Vicente Simão">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <link rel="shortcut icon" type="image/x-icon" href="<%= image_path('Bumbagora.png')%>" />
 
   <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
   <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
    <%= javascript_importmap_tags %>
  </head>

  <body class="d-flex flex-column h-100">
    <%= render 'layouts/header'%>
    <main>
      <% if !is_rooms_controller? and !is_device_controller?%>
        <%= render 'layouts/search_select' if @page != "home" %>
        <%= render 'layouts/search_bar'%>
      <% end %>

      <%= render 'layouts/message' if notice %>
      <%= render 'layouts/alert' if alert %> 
      <%= render 'layouts/info' if info %>
      
      <% if @page == "home" %>
        <%= render 'layouts/about' %>
        <%= render 'layouts/service' %>
      <% end %>
      
      <%= yield %>
    </main>
    <%= render 'layouts/footer' %>
  </body>
</html>
