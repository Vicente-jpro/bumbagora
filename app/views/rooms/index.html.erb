<h1>Meus chats</h1>

<div class="card">
  <div class="card-body" id="rooms">
    <% @rooms.each do |room| %>
      <%= render room %>
      <p>
        <%= link_to "Show this room", room %>
      </p>
    <% end %>
  </div>
</div>
